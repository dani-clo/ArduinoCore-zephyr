/*
 * Nano Chandler Monica uses the same Arduino variant implementation as the
 * bare Nano Chandler. Board-specific Serial mapping is selected through
 * ARDUINO_NANO_CHANDLER_MONICA in the shared variant headers.
 */
#include "../arduino_nano_chandler_pic32ck1025gc01144/variant.cpp"
